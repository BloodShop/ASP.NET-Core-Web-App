@page
@model ReverseEnginereeing.Pages.Countries.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Country[0].CountryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country[0].Alpha2Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country[0].Alpha3Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country[0].Numeric)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Country) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CountryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alpha2Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alpha3Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Numeric)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CountryId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CountryId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CountryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
